{
  "name": "retail-analytics-case-study",
  "version": "1.0.0",
  "description": "Explainable retail analytics and forecasting case study",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev": "concurrently --kill-others-on-fail --names api,web \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "npm install && npm --prefix frontend install && npm --prefix backend install",
    "python:setup": "python3 -m venv python-ml/.venv && python-ml/.venv/bin/python -m pip install --upgrade pip && python-ml/.venv/bin/pip install -r python-ml/requirements.txt",
    "data:build": "python-ml/.venv/bin/python python-ml/pipeline.py",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "db:migrate": "npm --prefix backend run migrate",
    "db:seed": "npm --prefix backend run seed",
    "db:setup": "npm run db:up && npm run db:migrate && npm run db:seed",
    "setup": "npm run install:all && npm run python:setup && npm run db:setup",
    "build": "npm run build:frontend && npm run build:backend && npm run typecheck:api",
    "test": "npm run test:python && npm run test:api && npm run test:frontend",
    "test:python": "PYTHONPATH=python-ml python-ml/.venv/bin/pytest -q python-ml/tests",
    "test:api": "backend/node_modules/.bin/tsx --test api/_analytics.test.ts",
    "test:frontend": "npm --prefix frontend test",
    "typecheck:api": "tsc --project api/tsconfig.json",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build"
  },
  "keywords": [
    "analytics",
    "dashboard",
    "ml",
    "react",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/node": "^5.8.22",
    "concurrently": "^8.2.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "mongodb": "^6.21.0"
  }
}
